nclusters = 4;rng(1000); % For reproducibility
[ T_summary, Table_Result ] = f_kmeans( 'Profiles-ANan-DEGs.csv', nclusters );

myDir = './; %gets directory
myFiles = dir(fullfile(myDir,'*.wav'); %gets all wav files in struct
for k = 1:length(myFiles)
  baseFileName = myFiles(k).name;
  fullFileName = fullfile(myFolder, baseFileName);
  fprintf(1, 'Now reading %s\n', fullFileName);
  [wavData, Fs] = wavread(fullFileName);
  % all of your actions for filtering and plotting go here
end

[ fig, ngene, expr,plotData, agis, agis_new ] = ...
    f_plotTable( sprintf('./ncluser%d/cluster%d',, transfile, varargin )
